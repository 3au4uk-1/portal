version: '3.8'

services:
  web:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro  # Добавлено :ro для безопасности
      - ./nginx-default.conf:/etc/nginx/conf.d/default.conf  # Кастомная конфигурация
    restart: unless-stopped