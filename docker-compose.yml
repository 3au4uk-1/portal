version: '3'

services:
  web:
    build: .  # This tells Docker to build using your Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - web-network

networks:
  web-network:
    driver: bridge