version: '3.8'

services:
  web:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./html:/usr/share/nginx/html
      - ./nginx-config:/etc/nginx/conf.d  # Монтируем всю папку с конфигами
    restart: unless-stopped