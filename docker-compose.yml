version: '3'

services:
  web:
    image: yourusername/my-webpage:latest
    ports:
      - "80:80"  # Map to port 8080 on host to avoid conflicts
    restart: unless-stopped
    networks:
      - web-network

networks:
  web-network:
    driver: bridge