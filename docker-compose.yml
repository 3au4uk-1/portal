version: '3'

services:
  web:
    image: nginx:alpine
    user: "nginx:nginx"
    container_name: my-webpage
    ports:
      - "80:80"
    restart: unless-stopped
    volumes:
      - ./html:/usr/share/nginx/html